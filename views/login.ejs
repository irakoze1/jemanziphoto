<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
            *{
                padding: 0;
                margin: 0;
                box-sizing: border-box;
            }

            .AdminContent{
                width: 100vw;
                height: 50vh;
                display: flex;
                flex-direction: column;
                justify-content: center;
                align-items: center;
                font-family: sans-serif;
            }

            label{
                margin-bottom: 10px;
                font-weight: bolder;
                color: #333;
                font-size: 20px;
            }

            #pw{
                padding: 10px 20px;
                border-radius: 5px;
                outline: none;
            }

            #adminSub{
                margin-top: 10px;
                padding: 7px 15px;
                border: 0;
                background: #333;
                color: #fff;
                border-radius: 5px;
                cursor: pointer;
            }

            #adminSub:active{
                transform: scale(1.2);
            }

            .alert{
                display: none;
                background: #F8D7DA;
                color: #721C24;
                padding: 10px 100px;
                margin-bottom: 10px;
                border-radius: 5px;
                transition: 0.2s;
            }
    </style>
</head>
<body>
    <form action="/login" method="POST">
        <div class="AdminContent">
            <div class="alert">Incorrect Password</div>
            <label for="password">Admin</label><input type="password" name="password" id="pw" placeholder="Password">
            <button id="adminSub">Sign in</button>
        </div>
       
    </form>
    <script></script>
</body>
</html>